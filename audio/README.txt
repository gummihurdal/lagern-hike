Put the track here as: track.mp3

The music control in the top bar only appears if this file exists, so the
page is safe to ship without it. To use a different filename or format,
change TRACK at the top of ../music.js.

Keep it under ~4 MB if you can — it loads on demand, but the whole site is
already 142 MB and GitHub Pages has a 100 GB/month bandwidth cap.
